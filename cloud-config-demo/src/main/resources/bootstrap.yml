server:
  port: 8089

spring:
  application:
    name: cloud-config
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8088/

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8090/eureka/
